# Development Guidelines

## Agent Protocol
- **Antigravity Agent Protocol**: ALL agents MUST strictly follow the definitions in [agent_protocol.md](file:///Users/sebastian/Developer/strategy-os/.agent/rules/agent_protocol.md).

## Tooling Preferences
- **Google Cloud**: ALWAYS use `gcloud-cli` (Homebrew Cask) instead of `google-cloud-sdk`. This applies to ALL projects.
  ```bash
  brew install --cask gcloud-cli
  ```

